## NOTES

- All test files must start with `test_` to be discoverable by unittest library.

## Commands

- `python3 -m http.server 8888` -> Python's built-in HTTP server to serve the contents of static files
